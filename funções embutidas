import random, math

n = int(input("Digite a quantidade de valores:"))
soma = 0  # Initialize soma to 0 before using it
for i in range(n):
    soma += random.randint(0, 100)  # Use randint, not randit
    print("A raiz quadrada da soma é", math.sqrt(soma))
